# Shopify Storefront API (live data)
# These match the variable names already set in the Vercel project.
# The token is a STOREFRONT API access token (not the Admin one).
SHOPIFY_STORE_DOMAIN=vercel-store-a45cd659.myshopify.com
SHOPIFY_STOREFRONT_ACCESS_TOKEN=

# Without a token the site renders from the baked-in catalog snapshot
# in src/lib/data/demo.ts — no crash, no blank page.
