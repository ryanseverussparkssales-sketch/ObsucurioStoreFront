# Shopify Storefront API (live data)
# Create a token: Shopify admin → Settings → Apps and sales channels →
# Develop apps → your app → Storefront API access token
PUBLIC_SHOPIFY_STORE_DOMAIN=vercel-store-a45cd659.myshopify.com
SHOPIFY_STOREFRONT_TOKEN=

# Without a token the site renders from the baked-in catalog snapshot
# in src/lib/data/demo.ts — no crash, no blank page.
